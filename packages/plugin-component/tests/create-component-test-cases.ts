import { createBlockTestCases } from './create-block-test-cases.js';

/**
 * Create test cases with component tags
 *
 * Currently, we are treating component tags as block tags
 */
export const createComponentTestCases = createBlockTestCases;
